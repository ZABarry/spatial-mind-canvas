/**
 * Intentionally unused: orbit pivot no longer follows the selected node (see `SceneCanvas`).
 * This file remains as a no-op so a running Vite dev session does not fail HMR after the feature
 * was removed. Safe to delete after restarting `npm run dev`.
 */
export function OrbitPivotEffect() {
  return null
}
